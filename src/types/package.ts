export type Package = {
  date: string;
  customerName: string;
  contact: string;
  product: string;
  subCategory: string;
  email: string;
  status: string;
  amount: string;
  earning: string;
};
